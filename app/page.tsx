/*
 * Foundation placeholder.
 *
 * Deliberately carries no human-readable copy. Per the seven rules, every
 * string on this site comes from Supabase — so there is nothing legitimate to
 * put here until the query layer (Phase 0.5) and the database-rendered shell
 * (Phase 0.7) exist. Pages are built last, on purpose.
 *
 * Replaced by the Landing page in Phase 1.
 */
export default function Home() {
  return <main />;
}
